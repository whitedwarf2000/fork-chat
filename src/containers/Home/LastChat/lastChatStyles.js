import styled from 'styled-components';
import { BaseFlex } from 'components/BaseStyles';

export const LastChatWrapper = styled(BaseFlex)`
  justify-content: space-between;
  margin: 1em 0;
`;

export const AddNewConversation = styled(BaseFlex)``;
